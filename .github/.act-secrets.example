# Secrets for local `act` runs.
# Copy this file to .github/.act-secrets and fill in real values.
# NEVER commit the actual secrets file.
#
# Required only for the release workflow (tsffer/tsflink steps).
# The check and nightly workflows do not need any secrets.
GITHUB_TOKEN=<your-github-personal-access-token>
